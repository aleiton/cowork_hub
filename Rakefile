# frozen_string_literal: true

# =============================================================================
# RAKEFILE
# =============================================================================
#
# Rake is Ruby's make-like build utility. Rails provides many built-in tasks:
#
#   rake -T                  # List all tasks
#   rake db:migrate          # Run migrations
#   rake db:seed             # Load seed data
#   rake db:reset            # Drop, create, migrate, seed
#   rake routes              # List all routes
#
# Custom tasks can be added in lib/tasks/*.rake
#
# =============================================================================

require_relative 'config/application'

Rails.application.load_tasks
